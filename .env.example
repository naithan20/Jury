# Only needed for local development — Vercel deployments get AI Gateway
# credentials automatically and don't need this set.
AI_GATEWAY_API_KEY=

# Optional: override the default model (anthropic/claude-haiku-4.5)
JURY_MODEL=
